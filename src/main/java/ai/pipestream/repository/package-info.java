/**
 * Central repository service: stores {@code PipeDoc} metadata and blobs, exposes gRPC and HTTP
 * upload APIs, and emits repository events for downstream processing.
 */
package ai.pipestream.repository;
