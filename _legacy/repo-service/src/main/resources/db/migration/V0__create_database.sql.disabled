-- Create database if it doesn't exist
-- This migration runs before all others to ensure the database exists
CREATE DATABASE IF NOT EXISTS pipeline_dev;

-- Grant all privileges to the pipeline user
GRANT ALL PRIVILEGES ON pipeline_dev.* TO 'pipeline'@'%';

-- Flush privileges to ensure they take effect
FLUSH PRIVILEGES;

-- Use the database for subsequent operations
USE pipeline_dev;

