# Gradle properties

org.gradle.jvmargs=-Xmx10g

# Pipestream BOM version - single source of truth for this consumer project
# Update this when upgrading to a new pipestream-platform release
pipestreamBomVersion=0.7.21-SNAPSHOT
